# Test Environment Configuration
# This file is used during test runs and should be committed to git

# Database Configuration (will be overridden by testcontainers)
DATABASE_URL=postgresql://test:test@localhost:5432/test

# Redis Configuration (will be overridden by testcontainers)
REDIS_URL=redis://localhost:6379/0

# JWT Configuration
SECRET_KEY=test-secret-key-for-ci-only-min-32-characters-long
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Email Configuration
EMAIL_BACKEND=console
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@efektywniejsi.pl
SMTP_FROM_NAME=Efektywniejsi

# Frontend Configuration
FRONTEND_URL=http://localhost:5173

# Password Reset Configuration
PASSWORD_RESET_TOKEN_EXPIRE_HOURS=1

# CORS Configuration
BACKEND_CORS_ORIGINS=["http://localhost:5173"]

# API Configuration
API_V1_PREFIX=/api/v1
PROJECT_NAME=Efektywniejsi Ekosystem Auth API - Test
DEBUG=False

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE_MB=50

# Mux Configuration (test values)
MUX_TOKEN_ID=test-mux-token-id
MUX_TOKEN_SECRET=test-mux-token-secret
